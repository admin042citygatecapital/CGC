import crypto from 'node:crypto';
import type {Request,Response} from 'express';
import {syntheticDisputes,SyntheticDisputeError,type DisputeActor} from '../../../lib/syntheticDisputes.js';
const serialize=(value:unknown)=>JSON.parse(JSON.stringify(value,(_key,item)=>typeof item==='bigint'?item.toString():item));
export default async function handler(req:Request,res:Response){const session=req.adminSession!;const actor:DisputeActor={id:session.adminId,email:session.email,ip:req.ip,correlationId:String(req.get('X-Request-ID')??crypto.randomUUID()),actorType:'admin'};try{const exportCaseId=String(req.query.exportCaseId??'').trim();if(exportCaseId){const result=await syntheticDisputes.export(exportCaseId,actor);res.setHeader('Cache-Control','no-store');res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Content-Disposition',`attachment; filename="${result.record.filename}"`);res.setHeader('X-Evidence-SHA256',result.record.evidenceSha256);return res.send(result.content);}return res.json(serialize(await syntheticDisputes.list()));}catch(error){if(error instanceof SyntheticDisputeError)return res.status(error.status).json({error:error.message,code:error.code});console.error('synthetic.dispute.read.error',error);return res.status(500).json({error:'Dispute workspace could not be read.',code:'INTERNAL_ERROR'});}}
