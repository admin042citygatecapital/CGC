import fs from 'node:fs'; import path from 'node:path'; import { describe, expect, it } from 'vitest';
const read = (file: string) => fs.readFileSync(path.resolve(process.cwd(), file), 'utf8');
describe('customer bill schedule boundary', () => {
  it('scopes every record to the authenticated customer', () => { const get=read('src/server/api/users/bills/GET.ts'); const post=read('src/server/api/users/bills/POST.ts'); expect(get).toContain('WHERE user_id = ${customer.id}'); expect(post).toContain('AND user_id=${customer.id}'); expect(post).not.toContain('req.body?.userId'); });
  it('cannot execute payments or modify balances', () => { const migration=read('src/server/db/migrations/0037_customer_bill_schedules.sql'); const post=read('src/server/api/users/bills/POST.ts'); expect(migration).not.toMatch(/(?:INSERT|UPDATE|DELETE)\s+(?:INTO|FROM)?\s*(?:journal|ledger|transaction)/i); expect(post).not.toMatch(/(?:journal|ledger|balance)(?:Account|Entry|Line|Minor)/); });
  it('is centrally authenticated and exposed through CustomerOnly pages', () => { const entry=read('src/server/entry.ts'); const routes=read('src/routes.tsx'); expect(entry).toContain('app.get("/api/users/bills"'); expect(entry).toContain('app.post("/api/users/bills"'); expect(routes).toContain("path: '/dashboard/bills'"); expect(routes).toContain('<CustomerOnly><FeatureOnly feature="payments"><DashboardBills /></FeatureOnly></CustomerOnly>'); });
});
